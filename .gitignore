# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/

# Environment (NEVER commit real .env files)
.env
.env.local
.env.*.local
apps/**/.env
apps/**/.env.*

# Note: .env.development and .env.production are TEMPLATES and ARE committed
# They contain safe defaults, not real secrets

# IDE
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Database
apps/api/data/
data/honeydo.db
data/errors.json
data/*.db

# Docker persistent data (bind mounts - back up separately!)
docker/data/

# Docker secrets (NEVER commit production secrets!)
docker/secrets/.env.prod
docker/secrets/*.prod
docker/secrets/*.secret

# Backups (store off-machine!)
backups/

# Cache
.cache/
.turbo/

# Testing
coverage/

# PWA
dev-dist/

# Drizzle
apps/api/drizzle/

# pnpm
.pnpm-store/

# TLS/SSL certificates (never commit private keys)
certs/
*.pem
*.key
*.crt
*.p12
*.pfx

# Windows artifacts
nul

# Playwright MCP
.playwright-mcp/
